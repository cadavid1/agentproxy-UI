# Gemini API Key (required)
GEMINI_API_KEY=your-api-key-here

# Claude binary path (optional, defaults to 'claude' from PATH)
# Use this to specify a local build or specific version
#CLAUDE_BIN=/path/to/.claude/local/claude

# OpenTelemetry Configuration
AGENTPROXY_ENABLE_TELEMETRY=0  # Set to 1 to enable telemetry
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
OTEL_SERVICE_NAME=agentproxy
#OTEL_SERVICE_NAMESPACE=  # Defaults to {user}.{project}

# Multi-Tenant Configuration
#AGENTPROXY_OWNER_ID=  # Defaults to current user
#AGENTPROXY_PROJECT_ID=default
#AGENTPROXY_ROLE=supervisor  # supervisor or worker

# Metric Export Configuration
#OTEL_METRIC_EXPORT_INTERVAL=10000  # milliseconds
